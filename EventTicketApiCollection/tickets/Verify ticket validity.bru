meta {
  name: Verify ticket validity
  type: http
  seq: 7
}

get {
  url: http://localhost:8081/booking/api/tickets/verify/{{ticketId}}
  body: none
  auth: inherit
}

body:json {
  {
    "eventId": "UUID-do-evento",
    "quantity": 2
  }
  
}

script:pre-request {
  const cookie = bru.getVar("cookie");
  
  if(cookie) {
    req.setHeader("Cookie", cookie)
  }
}
