meta {
  name: Create Order
  type: http
  seq: 1
}

post {
  url: http://localhost:8081/booking/api/orders
  body: json
  auth: inherit
}

body:json {
  {
    "ticketIds": [
      "0af40067-9b81-1181-819b-8192a1920002"
    ]
  }
  
  
}

vars:post-response {
  orderId: res.body.orderId
}

script:pre-request {
  const cookie = bru.getVar("cookie");
  
  if(cookie) {
    req.setHeader("Cookie", cookie)
  }
}
