meta {
  name: Create Order
  type: http
  seq: 1
}

post {
  url: http://localhost:8081/booking/api/orders
  body: json
  auth: inherit
}

body:json {
  {
    "ticketIds": [
      "0af4002a-9b9b-1c48-819b-9b3dfb690002"
    ]
  }
  
  
}

vars:post-response {
  orderId: res.body.orderId
}

script:pre-request {
  const cookie = bru.getVar("cookie");
  
  if(cookie) {
    req.setHeader("Cookie", cookie)
  }
}
