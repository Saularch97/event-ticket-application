meta {
  name: Create Order
  type: http
  seq: 1
}

post {
  url: http://localhost:8081/booking/api/orders
  body: json
  auth: inherit
}

body:json {
  {
    "ticketIds": [
      "258771fa-d4d4-47c3-b2af-e34a5a8945ff"
    ]
  }
  
  
}

vars:post-response {
  orderId: res.body.orderId
}

script:pre-request {
  const cookie = bru.getVar("cookie");
  
  if(cookie) {
    req.setHeader("Cookie", cookie)
  }
}
