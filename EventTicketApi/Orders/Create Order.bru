meta {
  name: Create Order
  type: http
  seq: 1
}

post {
  url: http://localhost:8081/booking/api/orders
  body: json
  auth: inherit
}

body:json {
  {
    "ticketIds": [
      "0af4009d-9baa-1e17-819b-aa87b8530005"
    ]
  }
  
  
}

vars:post-response {
  orderId: res.body.orderId
}

script:pre-request {
  const cookie = bru.getVar("cookie");
  
  if(cookie) {
    req.setHeader("Cookie", cookie)
  }
}
